#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

#include <time.h>
#include <string>

static const unsigned int WIDTH = sf::VideoMode::getDesktopMode().width;
static const unsigned int HEIGHT = sf::VideoMode::getDesktopMode().height;
static const std::string SELECT = "sound/select.wav";

#endif
